allowBuilds:
  edgedriver: false
  esbuild: true
  geckodriver: false
  lefthook: true

overrides:
  # @wdio/tauri-service@1.2.0 pins @wdio/native-utils@2.4.0 exactly, but that
  # published version is missing the `installMockSyncOverride` export it
  # imports, so the service crashes on startup. 2.5.0 restores it. Safe to
  # drop once @wdio/tauri-service bumps its own pin.
  "@wdio/native-utils": "2.5.0"
